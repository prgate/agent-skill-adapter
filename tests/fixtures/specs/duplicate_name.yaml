apiVersion: adapter.prgate.io/v1alpha1
kind: EnvironmentSpec
metadata:
  name: demo
  version: 1.0.0
spec:
  environment:
    versions: ">=2.1.200,<2.2.0"
  validForDays: 90
  skillFields:
    - name: model
      kind: string
      effect: Model override for this skill.
      provenance:
        url: https://code.claude.com/docs/en/skills
        anchor: "#frontmatter-reference"
    - name: model
      kind: string
      effect: A second, conflicting record for the same key.
      provenance:
        url: https://code.claude.com/docs/en/skills
        anchor: "#frontmatter-reference"
