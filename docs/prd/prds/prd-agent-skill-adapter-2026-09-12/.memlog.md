# memlog: Agent Skill Adapter
- event: run started (intent=create, stakes=launch, mode=coaching)
- decision: existing docs/prd/PRD-001-agent-skill-adapter.md used as input document, not as base
- decision: entry point = Vision + Features (capability-first), stakes = launch
- decision: origin pain = prgate-kit (github.com/prgate/prgate-kit) skills work in Claude Code only, not Gemini/others; standards change fast, manual porting costly and unverifiable
- decision: hard constraint = adapter follows official provider docs, output deterministic and predictable
- decision: users = skill developers; CI automation for skill adaptation; skill validation
- decision: existing PRD-001 was auto-generated -> treated as low-trust input, not source of truth
- event: spawned research subagents (competitive landscape, provider spec docs)
- event: landscape research returned premise-breaking finding — Agent Skills (SKILL.md) published as open standard, adopted broadly; format transpilation niche largely closed. Real gap = silent degradation of Claude-Code-only frontmatter fields (allowed-tools, context, model, hooks, disable-model-invocation) ignored by other agents. Nearest prior art: Nexa-Language/Skill-Compiler (SKILL.md->IR->emitters), vercel-labs/skills (31.4k stars, distribution not transformation), several linters. AWAITING verification by spec research before treated as fact.
- event: landscape research part 2 — install-path fragmentation already solved by npx skills; free niche = deterministic feasibility gate (source-vs-target capability delta, severity, CI exit code); real transpilation only needed for non-SKILL.md targets (.mdc, copilot-instructions, .windsurfrules); risks = capability-matrix maintenance cost, Vercel could add `skills doctor`, standard convergence shrinks value over time (finite product lifetime)
- event: spec research confirms open standard at agentskills.io — required frontmatter name+description, optional license/compatibility/metadata/allowed-tools(experimental). Claude Code implements ~18 extra fields (context: fork, model, effort, hooks, agent, background, shell, paths, arguments, user-invocable, disable-model-invocation, disallowed-tools). Antigravity/Gemini and Codex document name+description only. Cursor rules use .mdc with applyTo-style globs; Copilot instructions require applyTo. Limits differ: Claude 5000 tok/skill + 25000 budget; Codex AGENTS.md 32 KiB, skill list <=2% context.
- event: spec research part 2 — compatibility gaps (Claude Code has, targets lack): arguments/$ARGUMENTS, shell context injection !`cmd`, hooks in frontmatter (block, do not convert), subagents (only Antigravity, different schema), permissionMode/maxTurns/isolation/memory, allowed-tools patterns Bash(git *), context: fork + agent, model/effort (Antigravity only flash|pro), disable-model-invocation (partial in Cursor). Windsurf now Devin Desktop, hard limits 6000/12000 chars. .agents/skills/ is the converging path across Codex, Cursor, Copilot, OpenHands, Gemini CLI, Antigravity. Claude Code drifts fastest, fields version-pinned.
- event: spec research gaps 9-12 — disable-model-invocation partial in Cursor only (side-effect skill converted to always-on rule changes semantics); mcpServers subagent dependency not portable, breaks silently; progressive disclosure of references/assets lost in Copilot and Windsurf (inline blowup or file loss); size limits force a splitter not a translator (Windsurf 12000 chars/file, Codex AGENTS.md 32 KiB, skill list ~8000 chars)
- event: research phase complete, both subagents idle
- decision: success = no manual rewriting; an agent or CI run ports skills and other tools to other providers
- decision: proof of correct conversion = eval test results (behavioural), not just static validation
- decision: degradation policy chosen by user at the start of adaptation (convert with degradation vs workaround/polyfill vs refuse) — exact options deferred
- decision: direction at start = Claude -> Gemini only; broader matrix later
- decision: non-goal = authoring new skills; adapter only converts existing ones
- decision: scope is NOT skills only — subagents, hooks, roles and other primitives hidden behind the spec are in scope; these are exactly the non-portable parts (hooks portable nowhere, subagents only partially to Antigravity)
- event: vercel-labs/skills analysis (v1.5.25) — MIT, CLI only, no programmatic API (bin only, no exports). src/agents.ts = hand-maintained TS registry of 79 agents with install paths + detectInstalled, no capability data. Compatibility matrix is prose only (README:496-507, 18 agents x 4 rows: basic/allowed-tools/context:fork/hooks), unused by code. Install = symlink into .agents/skills canonical dir, fallback copy; content never transformed; frontmatter validation only requires string name+description. CI surface: --yes/--all/--json, skills-lock.json schema v3. Only extension point is host provider registry (in-memory, code-only). Relevant paths: antigravity ~/.gemini/antigravity/skills, gemini-cli ~/.gemini/skills, project path .agents/skills for all three.
- decision (proposed, pending user): reuse from vercel-labs/skills = agent path registry data (src/agents.ts) + compatibility table as seed for our machine-readable matrix; call `npx skills add --json -y` as external delivery step instead of reimplementing install/symlinks; do NOT fork the CLI, do NOT depend on the npm package (no exports, our product is Python)
- decision: user confirms positioning — adapter is not another installer; core value is analysis and transfer of primitives the spec does not cover (hooks, subagents, roles); delivery delegated to `npx skills`
- decision: sequencing — first formalize documentation/contracts (per-provider capability contract + the adapter's own contract) from official docs; scope list and degradation policy decided only after contracts are fixed
- decision: degradation policy selected by human via config file or CLI flag; in CI a default value applies (no human)
- decision: first target = Gemini, single surface, most popular one (Gemini CLI vs Antigravity to be pinned when contracts are written)
- open: unit of input (repo vs single skill) — under discussion
- open: evals ownership (adapter runs them vs emits artifact for external harness) — under discussion
- decision: input unit = repository only (.claude/ and siblings); standalone-skill-without-repo case is out of MVP scope
- decision: verification split — deterministic conformance tests validate the conversion (owned by adapter, CI-gated); evals already exist in prgate-kit and stay a separate optional manual behavioural check, not part of the adapter pipeline
- decision: legacy auto-generated PRD-001 removed from docs/prd/, archived as workspace input (inputs/PRD-001-legacy-autogenerated.md); docs/prd/ holds one source of truth
- decision: tests/unit/test_docs_integrity.py deleted — it asserted file existence and document headings, cannot fail on a behaviour defect
- decision: repo rule added in AGENTS.md — tests only where they can fail on real behaviour breakage; no tests asserting documentation structure
- event: RnD extract (prgate-kit/docs/research/2026-09-09-cross-provider-skill-adapter-rnd.md v0.0.5) — adapter contract: CLI plan/convert/check (+sync undefined), 5-stage pipeline (Source Conformance -> Target Spec Pre-flight -> Feasibility Gate -> Execution -> Target Conformance Gate), no disk writes before approval, feasibility_report.md or JSON with verdict+score+capability matrix (EXACT/ADAPTED/DEGRADED/BLOCKED)+semantic mapping ledger+diagnostics, INCOMPATIBLE returns non-zero exit and writes nothing. SIR = intent/triggers + required capabilities + abstract AST sections + resource manifest (no field schema yet). Determinism proven by golden-master AST snapshots (pytest+syrupy, byte-exact), target spec validator, leak detector, link integrity; offline, 0 LLM calls, <5s. Optional --smart uses Gemini Flash for prose rewrite (breaks determinism, opt-in). Provider contract = 3 layers (syntax/topology, tool primitives, runtime/environment) stored as specs/<vendor>/<version>/spec.yaml, versioned per vendor release (anthropic.agent-skills@1.0.0)
- event: RnD extract part 2 — skill verdicts COMPATIBLE / COMPATIBLE_WITH_DEGRADATION (needs [Y/n] or --allow-degraded) / INCOMPATIBLE; capability statuses EXACT/ADAPTED/DEGRADED/BLOCKED; strategies exact|adapted(+lowering_rules)|polyfill declared per capability in contracts/*.yaml, no selection algorithm; compatibility score 0-100 mentioned but formula undefined; provenance and deprecation policy ABSENT in RnD (gap to fix in PRD)
- event: RnD decisions already taken — Claude Code format is source of truth (no abstract meta-standard); contract-first validation both ends; fail-closed, no silent degradation; hybrid engine (deterministic AST default, LLM polish only under --smart); data-driven (translation rules in YAML, Python only executes; no hardcoded tools in core; mypy --strict); zero runtime overhead (compile at build/install, no runtime proxy); tests/ offline pre-commit gate vs evals/ paid manual/nightly in Docker; AGENTS.md <=150 lines single source of rules with CLAUDE.md/GEMINI.md as symlinks; stack uv/typer/rich/markdown-it/pydantic v2/ruff/mypy/pytest+syrupy
- event: RnD extract part 3 — Agentic SemVer defined (PATCH parser/polyfill fixes; MINOR new optional source fields, new polyfills, backwards-compatible compile; MAJOR orchestration paradigm change, gate tightening DEGRADED->INCOMPATIBLE, new major spec version). CI gate `make check-adapter` requires all repo skills stay COMPATIBLE. Roadmap v0.1 core, v0.2 commands/subagents/CLAUDE.md->GEMINI.md, v0.3 --smart+eval, v1.0 Codex/Cursor + bidirectional
- event: RnD conflicts with research — (a) treats `dependencies` as a spec frontmatter field, spec has only name+description; (b) hooks not mentioned anywhere in the document, whole asset class ignored; (c) treats subagents as portable ADAPTED capability, ignores that Codex/Cursor (v1.0 targets) have none and Antigravity schema differs; (d) emits to ~/.gemini/config/skills/, does not adopt shared .agents/skills path; (e) positions itself as replacement for the install layer, calls vercel-labs/skills a dumb downloader, has no scenario where npx skills delivers adapter output
- open: RnD leaves undefined — `sync` command semantics, compatibility score formula, specs/<vendor>/<version>/spec.yaml format, exit code table, provenance and deprecation policy
- decision (user, emphatic): no per-primitive policy decisions now (hooks, subagents, degradation strategies). They are made only after official specs are captured and mapping starts. PRD records them as explicitly deferred, not as open gaps to be guessed.
- decision: no implementation in this phase — PRD and shared product vision only
- change: PRD sections 6-11 drafted (capabilities A-D, 22 FRs, 6 NFRs, deferred decisions, risks, metrics with counter-metrics). No per-primitive policy fixed; RnD contract mechanics kept at requirement level only.
- change: PRD rewritten in plain Russian on user request — jargon replaced (провенанс -> источник сведений/дата проверки, эмиссия -> сборка под целевую среду, рантайм/таргет -> среда/целевая среда, ассет -> файл настроек, fail-closed -> при сомнении отказ, детерминизм -> предсказуемость). Meaning and requirement numbering unchanged.
- change: archived RnD source doc into workspace inputs/ alongside legacy PRD; inputs/README.md explains provenance and that prd.md is the single source of truth
- decision: contract staleness handled by keeping every environment-version description side by side with its capture date (no overwrite, past conversions reproducible); refresh runs automatically in CI and lands as a pull request reviewed by a human before merge. Added as FR-7 and FR-8; risk section updated.
- decision: environment descriptions are drafted by an LLM from official vendor docs and opened as a PR with a new version; human review is mandatory (hallucinated fields are the failure mode). Old version stays usable. LLM and network live only in this authoring step, never in the conversion path — NFR-2 reworded accordingly.
- decision: contract drafting maps against the official spec, so invented fields are largely structurally excluded; residual risks are omission, wrong support status, and stale doc pages — these are what human review and per-record source links exist to catch
- event: reconcile-legacy done (reconcile-legacy.md) — worth keeping: line-ending normalization for byte-identical output on Windows, path containment on write (../ in asset links), atomic write + no overwrite of existing files, numeric speed threshold, distinct exit codes for "could not read" vs "must not convert", explicit answer on library use and release milestones. Confirmed false in legacy PRD: tool matrix used Anthropic API primitives (bash_20241022, str_replace_editor) not Claude Code fields; XML tags and `parameters` frontmatter invented; Antigravity tool names/folders fabricated; target subagent and slash-command support asserted as fact
- event: reconcile-rnd done (reconcile-rnd.md) — gaps: (1) translation-rule version must be versioned separately from the two environment versions, otherwise FR-7 replay promise is incomplete; (2) output-side checks missing (validate against target schema, no source artefacts leaking, link integrity) — golden snapshots catch change, not consistently wrong output; (3) Agentic SemVer rules, especially "tightening a verdict breaks compatibility" for CI consumers; (4) skill body vs frontmatter boundary (markup, substitutions, resources) neither required nor deferred; (5) NFR-4 fail-closed never required to be tested
- event: rubric review done (review-rubric.md) — critical: "заключение" names two different objects (per file vs per run); vocabulary of statuses/verdicts deferred while FR-13..FR-17 depend on it; FR-4 has no staleness threshold value. High: three of seven deferred items do not actually depend on vendor specs; claimed differentiation lives entirely in deferred scope; §5 pins Gemini CLI while §9 calls the surface undecided
- decision: staleness is event-based, not time-based — scheduled CI check compares vendor doc version AND page content against what was captured; divergence marks the description stale (FR-4)
- decision: Gemini surface unpinned in section 5, moved to deferred list
- decision: skill body handling (tool names in prose, links, substitutions, length) deferred until real cases seen on prgate-kit; until then body carried as is, divergences reported
- decision: core is a library, CLI and CI step are thin wrappers; library API internal until first stable release; machine-readable report stabilizes first; GitHub Actions step ships in the first version as the external integration surface (FR-30, FR-31)
- change: applied ten review fixes — split verdict vocabulary (оценка / вывод по файлу / итог запуска), line-ending normalization, write containment, atomic write, distinct exit codes, output-side validation before write, translation-rule versioning, tightening-breaks-compatibility release rule, fail-closed must be tested, numeric speed threshold. FR count now 32.
- decision: hooks, subagents and permission/tool restrictions are all in scope of the first version — parsed, evaluated, reported (FR-16). Only the handling policy (block / substitute / external wrapper) stays deferred. This is what separates the product from a syntax checker.
- decision: core value statement (killer feature) — everything the spec does not cover goes through this tool and gets an explicit outcome: carried with substitution, moved outside as a separate mechanism, or refused with a stated reason. Nothing may disappear silently. Written into section 2.
- event: PRD finalized — status: final, published to docs/prd/PRD-001-agent-skill-adapter.md, source paths in section 12 rewritten relative to docs/prd/, README.md and docs/README.md repointed. Polish pass via bmad-review skipped: skill not installed in this project.
- decision (from prgate-kit#359): skill body work moved from deferred into the first version — rewrite tool names inside instructions by the target's mapping table, normalize markup, treat size limit as an environment property, split oversized skills into a short main file plus side files, repair links afterwards (FR-23..FR-27). Evidence: pr-loop/SKILL.md 127 KB (~32k tokens), pr-publish 54 KB, pr-review 43 KB; Antigravity limit 15 KB per skill
- decision: first target surface pinned to Google Antigravity (was undecided "Gemini"); removed from deferred list
- open: substitutions such as $ARGUMENTS with no target equivalent — leave as is with a report note, or expand into text; stays deferred
- note: Antigravity tool names quoted in #359 (run_command, view_file, write_to_file) must be verified against antigravity.google/docs/skills before entering the environment description — the legacy PRD asserted unpublished names
- event: bmad-review run on published PRD — four lenses (adversarial 15, edge-case 27, structure 18, prose 18). Report at review-lenses.md. Findings about undecided Gemini surface and deferred skill-body work are already obsolete after the #359 edits.
- change: PRD rewritten after lens review. Structure: §6 merged into §7 with one-line intros per group; summary block added at top; terms moved to §3 before first use; audience before non-goals; §5 condensed; risks unified to "риск -> что делаем -> FR"; process artefacts dropped from sources.
- change: 15 new requirements from review — explicit environment versions (FR-4), description pinning and selection rule (FR-5), significant-divergence and source-unavailable rules (FR-7), staleness flag committed to the file (FR-8), read containment and symlinks (FR-14), YAML parse limits (FR-15), structural break vs unknown field split (FR-16), out-of-repo settings listed (FR-17), empty-repo outcome (FR-18), cycle-safe link walk (FR-19), hook evaluated at attachment point only (FR-22), secret masking (FR-25), separate streams + report always emitted (FR-26), eight distinct exit codes (FR-27), determinism details (FR-29), generated-output-not-editable + orphans (FR-40), canonical output format named and versioned (FR-43), check-only mode (FR-45), un-tightening announced (FR-47)
- decision: terminology — product is called "адаптер"; "инструмент агента" reserved for agent tools
- decision: metrics given targets (80% skills without manual fixes, under a working day, 95% losses caught by report, at most one environment version behind); NFR-3 defined as 200 settings files in five seconds
- decision: vocabulary of statuses is fixed first among the deferred items, before report format and exit codes are called stable
