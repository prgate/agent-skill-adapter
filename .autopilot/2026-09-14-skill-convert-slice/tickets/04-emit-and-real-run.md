# 04 — Сборка в раскладку Antigravity и прогон на настоящем скилле

**Требования:** R01, R06 (половина W2), R10, R12, R19i, G02
**Blocked by:** 03
**Зона:** `src/agent_skill_adapter/convert.py`, `tests/unit/test_convert.py`, `docs/`
**Волна:** 3
**Status:** ready

## Что должно заработать

Та же команда с `--out <куда>` раскладывает скилл по тем путям, которые называет описание
Antigravity, — и это первый раз, когда из скилла Claude Code получается скилл для Gemini.

## Из брифа, дословно

> «сборка в раскладку Antigravity по layout-записям»
> «Раскладка для эмиттера уже посчитана и лежит в `specs/gaps/`… Писать её заново не нужно.»
> «конечная цель конвертер скилла claude to gemini»
> «W2. Переносить hook буквально и помечать „запрет не подтверждён“»

## Разделы спецификации

Истории 6, 11–12, 16; Решения §«Claude to Gemini», §«Уровень раскладки», §«Раскладка берётся
из `layout`», §«Готовая запись hook кладётся отдельным файлом».

## Критерии приёмки

- [ ] Целевые пути берутся из `LayoutEntry.path` описания цели; в коде `convert.py`
      нет ни одного литерального пути целевой среды
- [ ] `--scope project|user` выбирает запись `skills.project` / `skills.user`;
      по умолчанию `project`
- [ ] Подстановки `<skill-name>` и `<workspace-root>` разворачиваются; `~` в пути
      пользовательского уровня разворачивается в домашнюю папку
- [ ] `SKILL.md` и `scripts/` собраны; папка, которой в описании цели нет
      (`references/`, `assets/`), не переносится молча — её судьба видна в отчёте
- [ ] Файл на месте назначения уже есть — код 8, ничего не переписано
- [ ] Hook с воспроизводимым событием: запись легла в `<out>/hooks.json`, а отчёт назвал
      путь назначения из описания (`.agents/hooks.json`) и сказал, что вписать её туда — вручную
- [ ] Отчёт перечисляет, что именно записано (`written`), и без `--out` этот список пуст
- [ ] Прогон на настоящем скилле из `~/.claude/skills/`: команда, код возврата и вывод
      записаны в репозиторий (`docs/` или `.autopilot/reports/`), результат читаем человеком
- [ ] `make check` зелёный
