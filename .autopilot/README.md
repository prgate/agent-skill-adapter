# Как читать эту папку

- `dashboard.html` — открывается сам в начале сборки; можно и двойным кликом.
  Этапы, прогресс, время, что осталось. Обновляется сам, пока сборка идёт.
  `index.html` рядом — тот же файл под другим именем, чтобы дашборд открывался
  по короткому адресу. Отдельно его открывать не нужно.
- `<дата>-<проект>/` — папка одной сборки. Дата в имени — день, когда сборка началась,
  поэтому папки идут по порядку сверху вниз. Суффикс `--wip` значит «ещё делается»;
  когда сборка сдана, суффикс снимается. Внутри:
  - `<дата>-brief.md` — твоя изначальная задача, слово в слово, и «Дополнения» — всё,
    что ты сказал позже. Сам текст задачи не переписывается.
  - `manifest.md` — список требований и что с каждым стало.
  - `spec.md` — спецификация.
  - `tickets/` — таски, на которые разбита сборка (если сборка мелкая, их нет).

Если сборка прервалась — скажи агенту «продолжи автопилот», он поднимет состояние отсюда.

## Прогоны

| Начат | Папка | Статус | Итог |
|---|---|---|---|
| 2026-09-14 | `2026-09-14-env-specs-gate` | сдан | Два машиночитаемых описания сред (Claude Code и Antigravity) со ссылками, якорями и хешами разделов документации; загрузчик с выбором по версии и отказом на устаревшем описании; сверка с документацией поставщика; вычисляемый перечень пробелов — ответ на FR-48. |
| 2026-09-14 | `2026-09-14-skill-convert-slice` | сдан | Команда `convert`: папка скилла Claude Code читается, каждое её свойство получает оценку по описаниям сред, и скилл собирается в ту раскладку, которую называет описание Antigravity — с отчётом, кодом возврата и перечнем того, что не перенеслось. Плюс три решения PRD §5, которые это разблокировали. |
| 2026-09-14 | `2026-09-14-pr49-findings` | сдан | Два входа, на которых команда печатала трассировку, не выпускала отчёта и выходила кодом «перенёс с потерями», теперь дают отказ с отчётом и своим кодом; заголовок скилла нормализуется так, что `hooks.json` одинаков на одинаковом входе. Плюс политика языка в `AGENTS.md` описывает репозиторий, а не спорит с ним. |
